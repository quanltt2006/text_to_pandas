# Frontend (Vite) — dùng cho production build
# Trỏ tới URL backend đã deploy trên Render (thêm "/api" ở cuối).
# VD: VITE_API_BASE=https://text-to-pandas-backend-xxxx.onrender.com/api
#
# Dev local: KHÔNG cần đặt. Mặc định dùng /api và Vite proxy tới http://localhost:8000.
VITE_API_BASE=